{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "nix-env -iA nixpkgs.python311Packages.pip && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
